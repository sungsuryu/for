<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.knia.foreign.exchange.dao.mapper.UserMapper">

	<select id="countUser" resultType="int">
			SELECT
				COUNT(USER_ID) FROM TB_FOR_USER
	</select>

	<select id="selectUser" parameterType="loginVO" resultType="egovframework.knia.foreign.exchange.vo.UserVO">
			SELECT
				USER_ID, USER_NAME, PASSWORD, 
				CMPY_CD, EML_ADDR, CELL_NUM
			FROM
				TB_FOR_USER
			WHERE
				USER_ID=#{loginId} 
				AND PASSWORD=#{password}
				AND IS_DEL='N' 
				AND USE_YN='Y' 
				AND IS_APPROVE='Y'
	</select>
</mapper>